# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
# Calllie uses these credentials to connect to your MySQL/MariaDB database.
# If you are on a shared host (cPanel), these details are in your "MySQL Databases" section.
# If you are on XAMPP, the default user is usually 'root' with no password.
# -----------------------------------------------------------------------------
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=callie_production
DB_USER=root
DB_PASSWORD=

# -----------------------------------------------------------------------------
# APPLICATION SETTINGS
# -----------------------------------------------------------------------------
# APP_ENV: Set to 'development' to see full error traces.
#          Set to 'production' to hide errors and show generic 500 messages.
APP_ENV=development

# -----------------------------------------------------------------------------
# SECURITY
# -----------------------------------------------------------------------------
# CORS_ORIGIN:
#   *         = Allow all websites (Good for development)
#   https://yoursite.com = Allow only your frontend (Essential for production)
CORS_ORIGIN=*

# JWT_SECRET:
#   This key signs your authentication tokens. 
#   CHANGE THIS to a random long string before deploying!
JWT_SECRET=change-this-to-a-random-string-in-production
